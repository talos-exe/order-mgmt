@using X.PagedList
@using X.PagedList.Mvc.Core
@model List<OrderMgmtRevision.Models.Product>
@{
    ViewBag.Title = "Products";
}

<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">

<h2 class="text-center mb-4 pt-4">Product Management</h2>

<ul class="nav nav-tabs ms-5 me-5" id="productTabs">
    <li class="nav-item">
        <a class="nav-link active" id="productListTab" data-bs-toggle="tab" href="#productList">Product List</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" id="addProductTab" data-bs-toggle="tab" href="#addProduct">Add Product</a>
    </li>
</ul>

<div class="tab-content mt-3">
    <div class="tab-pane fade show active" id="productList">
        <div class="ms-5 me-5">
            <div class="d-flex justify-content-start mb-2">
                <button class="btn btn-primary btn-sm me-2" data-bs-toggle="modal" data-bs-target="#removeProductModal" disabled id="btnRemove">Remove</button>
                <input type="text" id="searchBar" class="form-control-sm me-2" placeholder="Search by SKU" onkeyup="filterTable()" />
            </div>

            <div class="table-responsive">
                <table class="table table-striped table-sm table-hover align-middle" id="productTable">
                    <thead class="table-light">
                        <tr>
                            <th><input type="checkbox" id="selectAll" onclick="toggleAll(this);"></th>
                            <th>SKU</th>
                            <th>Product Name</th>
                            <th>Price</th>
                            <th>Stock</th>
                            <th class="more">More</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var product in Model)
                        {
                            <tr onclick="toggleButtons(this)">
                                <td><input type="checkbox" class="product-checkbox" value="@product.ProductID" onclick="toggleButtons();"></td>
                                <td>@product.SKU</td>
                                <td>@product.ProductName</td>
                                <td>@product.Price.ToString("C")</td>
                                <td>@product.Stock</td>
                                <td>
                                    <a class="btn-sm btn-primary" style="cursor: pointer" onclick="openProductDetailsTab('@product.ProductID', '@product.ProductName')">View Details</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<!-- Remove Product Modal -->
<div class="modal fade" id="removeProductModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Confirm Product Removal</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <p>Are you sure you want to remove the selected products? This action <strong>cannot be undone</strong>.</p>
            </div>
            <div class="modal-footer">
                <form asp-action="RemoveProducts" asp-controller="Products" method="post">
                    <button type="submit" class="btn btn-danger">Yes, Delete</button>
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                </form>
            </div>
        </div>
    </div>
</div>

<script src="~/js/products.js"></script>
